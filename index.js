function addingEventListener(){
    const input = doument.getElementById('input');

    function clickAlert(){
    }
    input.addingEventListener('click',clickAlert);
}

//Call the addingEventListener function to set up the event listener
addingEventListener();